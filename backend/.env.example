MONGODB_URI=mongodb://127.0.0.1:27017/globaltna
PORT=4000
CORS_ORIGIN=http://localhost:3000
# Random hex string >= 32 chars. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
